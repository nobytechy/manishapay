# Noby Payments Knowledge Base — moved

The dataset now lives in its own repository (single source of truth), and is
published to Hugging Face:

**→ https://github.com/nobytechy/manishapay-dataset**

An open, CC BY 4.0, machine-readable knowledge base of 11 payment gateways —
auth, initiate/status flows, capabilities, payment methods, official SDKs, and
70 real developer pain-points with causes + mitigations. Same subject matter as
this backend's provider catalog (`backend/src/providers/`).

Do not add dataset records here — edit them in `manishapay-dataset` instead.
